
<h1 class="text-center mt-4">All categories on Livra-me</h1>
<div class="flickr_pagination">
  <%= will_paginate @categories, :container => false %>
</div>
<%= render 'category' %>
<div class="flickr_pagination mb-3">
  <%= will_paginate @categoriess, :container => false %>
</div>
