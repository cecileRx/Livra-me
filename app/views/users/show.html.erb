
  <h2 class="text-center mt-4"><%= @user.username%>'profile</h2>
  <div class="text-center">
   <%= link_to "Update your profile", edit_user_path(@user), class: "btn btn-outline-info" %>
  </div>
  <h3 class="text-center mt-4">My library</h3>
  <%= render 'books/book' %>
